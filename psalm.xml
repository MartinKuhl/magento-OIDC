<?xml version="1.0"?>
<psalm
    errorLevel="5"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    findUnusedBaselineEntry="false"
    findUnusedCode="false"
>
    <projectFiles>
        <directory name="Block" />
        <directory name="Controller" />
        <directory name="Helper" />
        <directory name="Model" />
        <directory name="Observer" />
        <directory name="Plugin" />
        <directory name="Logger" />
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>

    <issueHandlers>
        <!-- PHP 8.3+ Feature – nicht relevant für PHP 8.1 Minimum -->
        <MissingOverrideAttribute errorLevel="suppress" />

        <!-- Magento-Klassen werden via XML/DI referenziert, nicht direkt im Code -->
        <UnusedClass errorLevel="suppress" />

        <!-- Magento Factory-Klassen werden zur Laufzeit generiert -->
        <UndefinedDocblockClass errorLevel="suppress" />

        <!-- Magento Magic Methods (getData, setData, etc.) -->
        <UndefinedClass>
            <errorLevel type="suppress">
                <referencedClass name="Magento\User\Model\UserFactory" />
                <referencedClass name="Magento\Customer\Model\CustomerFactory" />
                <referencedClass name="Magento\Customer\Api\Data\AddressInterfaceFactory" />
                <referencedClass name="Magento\Directory\Model\ResourceModel\Country\CollectionFactory" />
                <referencedClass name="Magento\User\Model\ResourceModel\User\CollectionFactory" />
                <referencedClass name="Magento\Framework\HTTP\Adapter\CurlFactory" />
                <referencedClass name="MiniOrange\OAuth\Model\MiniorangeOauthClientAppsFactory" />
            </errorLevel>
        </UndefinedClass>

        <!-- Magento DI: Konstruktoren werden via di.xml aufgerufen -->
        <PossiblyUnusedMethod>
            <errorLevel type="suppress">
                <referencedMethod name="MiniOrange\OAuth\*::__construct" />
            </errorLevel>
        </PossiblyUnusedMethod>

        <!-- Magento Plugin-Parameter ($subject) sind Pflicht aber oft ungenutzt -->
        <PossiblyUnusedParam>
            <errorLevel type="suppress">
                <directory name="Plugin" />
            </errorLevel>
        </PossiblyUnusedParam>

        <!-- Magento Interface-Methoden haben nicht immer alle Methoden -->
        <UndefinedInterfaceMethod>
            <errorLevel type="suppress">
                <referencedMethod name="Magento\*::*" />
            </errorLevel>
        </UndefinedInterfaceMethod>
    </issueHandlers>
</psalm>
