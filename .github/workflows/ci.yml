name: Magento OIDC CI

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]

permissions:
  contents: write
  pull-requests: write


jobs:

  # -----------------------------------------------------
  # PHP QUALITY JOB (PHPCS + Auto-Fix via PHPCBF)
  # -----------------------------------------------------
  php:
    name: PHP QA (PHP ${{ matrix.php-version }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        php-version: [8.1, 8.2, 8.3, 8.4]

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}
          tools: composer, phpcs, phpcbf

      - name: Determine Composer Home
        run: echo "COMPOSER_HOME=$(composer global config home --absolute)" >> $GITHUB_ENV

      - name: Cache Composer Global
        uses: actions/cache@v4
        with:
          path: ${{ env.COMPOSER_HOME }}
          key: phpcs-magento-${{ runner.os }}-${{ hashFiles('.github/workflows/*.yml') }}
          restore-keys: |
            phpcs-magento-${{ runner.os }}-

      - name: Allow phpcodesniffer-composer-installer plugin
        run: composer global config --no-plugins allow-plugins.dealerdirect/phpcodesniffer-composer-installer true

      - name: Install Magento Coding Standard + PHPCompatibility Fork
        run: |
          composer global require magento/magento-coding-standard --no-interaction --no-progress
          phpcs --config-set installed_paths "$COMPOSER_HOME/vendor/magento/magento-coding-standard,$COMPOSER_HOME/vendor/magento/php-compatibility-fork"

      - name: Debug PHPCS Standards
        run: phpcs -i

      - name: PHP Compatibility Check (non-blocking)
        run: |
          phpcs --standard=PHPCompatibility \
                --runtime-set testVersion 8.1- \
                --extensions=php,phtml \
                --ignore=vendor/*,node_modules/*,.github/* \
                . || true

      - name: Auto-fix with PHPCBF
        id: phpcbf
        run: |
          # PHPCBF nutzt phpcs.xml automatisch (LiteralNamespaces ist dort excluded)
          # Exit-Codes: 0 = nichts zu tun, 1 = Fixes angewendet, 2 = Fixes + Rest, 3 = Fehler
          phpcbf . || PHPCBF_EXIT=$?
          PHPCBF_EXIT=${PHPCBF_EXIT:-0}

          if [ "$PHPCBF_EXIT" -eq 1 ] || [ "$PHPCBF_EXIT" -eq 2 ]; then
            echo "has_fixes=true" >> $GITHUB_OUTPUT
            echo "::notice::PHPCBF hat automatische Korrekturen angewendet."
          else
            echo "has_fixes=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request with PHPCBF fixes
        if: steps.phpcbf.outputs.has_fixes == 'true' && matrix.php-version == '8.2'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: autofix/phpcbf-${{ github.ref_name }}
          commit-message: "style: auto-fix PHPCS violations via PHPCBF (Magento2 Standard)"
          title: "Auto-Fix: PHPCS violations on `${{ github.ref_name }}`"
          body: |
            ## Automatische Code-Style-Korrekturen

            Dieser PR wurde automatisch vom CI erstellt.

            **Was wurde gemacht:**
            - PHPCBF hat alle automatisch korrigierbaren PHPCS-Violations behoben
            - Standard: **Magento2** (via `phpcs.xml`)

            **Trigger:** Push auf `${{ github.ref_name }}` (Commit ${{ github.sha }})

            > ⚠️ Bitte prüfe die Änderungen vor dem Merge.
          delete-branch: true

      - name: Magento2 Standard (blocking)
        run: phpcs .


  # -----------------------------------------------------
  # STATIC ANALYSIS JOB (PHPStan + Psalm)
  # -----------------------------------------------------
  static-analysis:
    name: Static Analysis (PHPStan + Psalm)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.2
          tools: composer

      - name: Determine Composer Home
        run: echo "COMPOSER_HOME=$(composer global config home --absolute)" >> $GITHUB_ENV

      - name: Cache vendor dependencies
        uses: actions/cache@v4
        with:
          path: vendor
          key: static-vendor-${{ runner.os }}-${{ hashFiles('.github/workflows/*.yml') }}
          restore-keys: |
            static-vendor-${{ runner.os }}-

      - name: Create temporary composer.json for Magento stubs
        run: |
          if [ -f composer.json ]; then
            cp composer.json composer.json.bak
          fi
          cat > composer.json <<'EOF'
          {
            "require-dev": {
              "phpstan/phpstan": "*",
              "bitexpert/phpstan-magento": "*",
              "magento/framework": "*",
              "magento/module-backend": "*",
              "magento/module-catalog": "*",
              "magento/module-customer": "*",
              "magento/module-store": "*",
              "magento/module-config": "*",
              "magento/module-ui": "*",
              "magento/module-authorization": "*",
              "vimeo/psalm": "*"
            },
            "repositories": [
              {
                "type": "composer",
                "url": "https://mirror.mage-os.org/"
              }
            ],
            "minimum-stability": "dev",
            "prefer-stable": true
          }
          EOF

      - name: Allow Magento Composer plugins
        run: |
          composer config --no-plugins allow-plugins.dealerdirect/phpcodesniffer-composer-installer true
          composer config --no-plugins allow-plugins.magento/composer-dependency-version-audit-plugin true

      - name: Install PHPStan + Magento Stubs + Psalm
        run: composer install --no-interaction --no-progress

      - name: Restore original composer.json
        run: |
          if [ -f composer.json.bak ]; then
            mv composer.json.bak composer.json
          else
            rm -f composer.json
          fi

      - name: Run PHPStan
        run: vendor/bin/phpstan analyse --memory-limit=1G || exit 1

      - name: Run Psalm
        run: vendor/bin/psalm --no-cache || exit 1
