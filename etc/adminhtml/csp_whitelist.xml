<?xml version="1.0"?>
<!--
/**
 * CSP Whitelist for MiniOrange OIDC Plugin (adminhtml)
 *
 * Provider hostnames are added at runtime by OidcCspPolicyCollector
 * based on the configured OIDC provider authorize URL.
 * Do NOT add customer-specific hostnames here — they would apply
 * to every installation that installs this module.
 */
-->
<csp_whitelist xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:module:Magento_Csp:etc/csp_whitelist.xsd">
    <policies/>
</csp_whitelist>